# unity-mcp — Workflow Reconciliation Handoff

Prepared 2026-09-11 for **Unity MCP package/server coordinator; local host settings owner**. This is a dated handoff for the
user-requested project-specific follow-through, not a second current status owner.
At pickup, refresh Git, the active task and actual decisions before editing.

**Bounded follow-through completed 2026-09-12.** The dated request below is
retained; [Coordinator Follow-Through](#coordinator-follow-through) supersedes
its pending-cleanup status. This receipt does not own live product acceptance.

## Read First

- [ai-rules/project-knowledge.md](../ai-rules/project-knowledge.md).
- [ai-rules/testing-and-verification.md](../ai-rules/testing-and-verification.md).
- [ai-rules/transports-and-routing.md](../ai-rules/transports-and-routing.md).
- [ai-rules/release-and-packaging.md](../ai-rules/release-and-packaging.md).
- [ai-rules/custom-tool-schema.md](../ai-rules/custom-tool-schema.md).

## Applied Rule Scope

The shared review was adapted to this project's existing owners: claim-scoped
completion, original request/decision preservation, continuing authorization,
and regression evidence from the production owner. UI reference/design semantics
apply only where this project has that surface. Existing operation/acceptance
boundaries remain in force. Review the actual local diff in:

- [`ai-rules/engineering-method.md`](../ai-rules/engineering-method.md).
- [`ai-rules/testing-and-verification.md`](../ai-rules/testing-and-verification.md).
- [`AGENTS.md`](../AGENTS.md).

## Local Findings And Questions

| Evidence status | Finding and implication |
| --- | --- |
| Verified working-tree boundary | The only pre-existing dirty path was .claude/settings.json. It is local host work and is preserved outside this documentation slice. |
| Local inspection needed | Check currently used release/tool-extension records for copied consumer pin or runtime readiness claims. A Python test, schema check or package commit does not prove Unity import/runtime in a consumer app. |
| Preserve ownership | C# discovery/handler metadata, Python registration, transport/session routing and consumer app pins have separate owners. A hypothetical probe path must not become a production reachability claim. |

## Next Bounded Pass

Start from the active package/server task and one affected tool/transport claim; reconcile its records and package evidence. Route any consumer qualification to its existing app owner rather than copying their full acceptance matrix into this repository.

For the selected work only: map original request -> actual decisions -> current
claim owner -> evidence -> next action. Keep current routing concise; preserve
consequential history, especially never-committed material, before removing it.
Different subclaims can have different statuses. Record unknown history as
unknown. If a second review finds the same duplication/staleness class, decide
keep/split/merge at the owner rather than adding another patch paragraph.

Done when the selected current routes reach one owner per claim, material old
instructions are explicitly superseded with their evidence preserved, and every
remaining gap has an owner and next action. Use scoped link/rule checks and a
read-through; this is not a mandate for a repository-wide audit or full test run.
If no drift is found, record that bounded result without manufacturing cleanup.

## Ownership, Evidence And Authority

Baseline HEAD: `239071d1e7b52bd5d142fa0648d932815f07b5f3`. Before this rollout there were **1** changed
paths and an empty index. These numbers are capture-time provenance, not current
status. Existing product/evidence/settings slices remain with their project
owners; preserve their contents and stage nothing from them as part of this
handoff. The workflow rollout owns only its new rule/document hunks and snapshots;
shared dirty files require hunk-level integration. Commit state is independent
of acceptance; no commit or push is requested by this handoff.

No consumer repin, .mcp.json/settings mutation, Unity run, package release, server restart or publication follows from this handoff.

The findings above come from local source/document inspection on 2026-09-11.
No product tests, live service checks or device checks were performed for them.
At the September 11 handoff, follow-through was **ready for the project
coordinator**, not completed. The receipt below supersedes that dated status.

## Coordinator Follow-Through

Completed 2026-09-12 at documentation scope; uncommitted. Selected work was the
active Editor-readiness/status-contract correction, not a repository-wide audit.
Owner: MCP/tooling coordinator. One bounded read-through and scoped document
validation; no additional runtime checks, commits, restarts or repins.

- Retained the reviewed root/engineering/test rule rollout. Corrected one stale
  verification instruction: a consumer's older pin alone does not prove that
  corrected local code cannot be verified. The rule now requires separate
  imported-package/assembly and running-server provenance, including uncommitted
  source hashes. An authorized local integration is not a published-pin claim.
- Consolidated selected live dispositions in the existing ignored tooling
  incident record; the private backlog routes there. Source regression,
  imported Unity compilation/test, live stdio service, persistent-client
  adoption and Pipeline causality are separate claims. Historical recovery and
  completed fixture instructions are no longer presented as current actions.
- Kept release/pin and schema ownership in their existing rule modules. The
  selected generated status reference agrees with the distinction between
  diagnostic success and readiness. No additional drift was found there;
  no consumer acceptance matrix was copied into the fork.
- Preserved exact pre-edit handoff and private-record bytes with SHA-256 receipts
  under ignored local history before cleanup. Original requests, approvals,
  holds, failure evidence and unknown incident cause remain recoverable.
  Consumer identities, paths and diagnostics remain private, not in this receipt.
- Reconciled current changed-path ownership in the existing private inventory.
  Host settings and application slices remain with their owners. Completed
  source acceptance is distinct from uncommitted work and client adoption.

Current-owner routing: the testing rule owns loaded-code evidence; transport
rules own discovery/readiness procedure; the existing private incident owns
integration disposition and Pipeline follow-up; release/pin decisions stay with
release and consumer coordinators. This dated receipt adds no product gate.

Validation passed: rule-map, scoped whitespace, 20 local document links/anchors,
five preserved snapshots and 17 unchanged protected files. The prior product-test evidence is retained,
not rerun or promoted by this documentation pass. No new user app test is needed.
